<h1 class="display-4 text-center mt-4">Perfil {{user.nombre}}</h1>
<hr class="hr-divider">

<div class="container text-center">
    <div class="card">
        <div class="card-body">
            <h2 class="card-title">Información del Usuario</h2>
            <ul class="list-group list-group-flush">
                <li class="list-group-item h4"><strong>Nombre:</strong> {{user.nombre}}</li>
                <li class="list-group-item h4"><strong>Correo:</strong> {{user.email}}</li>
                <li class="list-group-item h4"><strong>Rol:</strong> {{user.rol}}</li>
            </ul>
        </div>
    </div>
</div>

<div class="container text-center mt-4">
    <h2>Dirigirse a:</h2>
    <a href="/" class="btn btn-secondary btn-lg mr-2">Home</a>
    <a href="/login" class="btn btn-secondary btn-lg mr-2">Login</a>
    <a href="/register" class="btn btn-secondary btn-lg mr-2">Registro</a>
    <a href="/perfil" class="btn btn-secondary btn-lg">Perfil</a>
    <a href="api/sessions/logout" class="btn btn-secondary btn-lg">Logout</a>
</div>