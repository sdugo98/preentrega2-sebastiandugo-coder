<h1 class="display-4 text-center mt-4">Perfil {{user.last_name}}</h1>
<hr class="hr-divider">

<div class="container text-center">
    <div class="card">
        <div class="card-body">
            <h2 class="card-title">Información del Usuario</h2>
            <ul class="list-group list-group-flush">
                <li class="list-group-item h4"><strong>Nombre:</strong> {{user.first_name}}</li>
                <li class="list-group-item h4"><strong>Correo:</strong> {{user.email}}</li>
                <li class="list-group-item h4"><strong>Rol:</strong> {{user.rol}}</li>
            </ul>
        </div>
    </div>
</div>

{{!-- {{>menu1}} --}}
{{>menu2}}